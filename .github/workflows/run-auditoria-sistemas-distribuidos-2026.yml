name: One-time audit and reconcile UAQ Sistemas Distribuidos
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-auditoria-sistemas-distribuidos-2026.yml'
permissions:
  contents: read
concurrency:
  group: academic-reconciliation-sistemas-distribuidos-2026
  cancel-in-progress: false
jobs:
  audit-and-reconcile:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm ci
      - name: Verify deployment is configured
        shell: bash
        run: |
          test -n "$(node -p "JSON.parse(require('fs').readFileSync('.apps-script-deployment.json','utf8')).deploymentId")"
      - name: Audit and reconcile concluded classes in UAQ Sistemas Distribuidos
        env:
          GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.GOOGLE_OAUTH_CLIENT_ID }}
          GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.GOOGLE_OAUTH_CLIENT_SECRET }}
          GOOGLE_OAUTH_REFRESH_TOKEN: ${{ secrets.GOOGLE_OAUTH_REFRESH_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          npm run apps-script:run -- --function auditarYCompletarMateriaAcademica --parameters-json '["871158466533"]'
